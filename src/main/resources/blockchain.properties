# Blockchain Configuration

# Provider URL
blockchain.provider.url=http://localhost:8545

# Network ID
blockchain.network.id=1337

# Gas settings
blockchain.gas.limit=6721975
blockchain.gas.price=20000000000

# Confirmation settings
blockchain.confirmation.blocks=1
blockchain.timeout.ms=30000

# Implementation settings
blockchain.mock.enabled=false
blockchain.testnet.enabled=true
blockchain.production.enabled=false

# Cosmos Testnet settings
blockchain.cosmos.rest.endpoint=http://localhost:1317
blockchain.cosmos.rpc.endpoint=http://localhost:26657
blockchain.cosmos.chain.id=quicktest

# System account settings
blockchain.system.account.address=0x0123456789abcdef0123456789abcdef01234567
blockchain.system.account.privateKey=0x21d1f574cb16b188b38df1edd82cc99f2c14dea0bb192a7389732c2f28e92c7b

# Contract settings
blockchain.contract.address=0x0123456789abcdef0123456789abcdef01234567
blockchain.contract.auto.deploy=false
blockchain.contract.abi=[]
blockchain.contract.bytecode=0x

# Retry settings
blockchain.retry.max.attempts=3
blockchain.retry.delay.ms=1000

# Event settings
blockchain.events.enabled=true
blockchain.events.polling.interval.ms=15000

# Transaction receipt settings
blockchain.transaction.receipts.enabled=true

# Sync settings
blockchain.sync.enabled=true
blockchain.sync.interval.ms=60000

# Metrics settings
blockchain.metrics.enabled=true

# Transaction batching settings
blockchain.transaction.batching.enabled=false
blockchain.transaction.batching.max.size=10
blockchain.transaction.batching.max.wait.ms=5000
